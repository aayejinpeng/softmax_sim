# Multi-Kernel Benchmark Results

Generated by `python3 softmax_simulator.py --benchmark`.

## Configuration

- benchmark source: `/root/opencute/opencute_github/softmax_sim/plans/benchmark_config.example.yaml`
- execution mode: `in-order`
- register width: `2048` bits
- sequence chunk: `2048` bits
- rows per non-softmax kernel: `8`
- softmax heads: `8`
- cache bandwidth: `64` bytes/cycle
- compute widths: reduce/simple/complex = `512`/`512`/`512` bits
- lane strides: `0, 0, 0, 0` bytes
- exp2 unit for softmax: `False`

Speedup is normalized per kernel to `lanes=1, issue_width=2` when present; otherwise the first case for that kernel is used.

## Summary

| case | description | kernel | lanes | issue width | cycles | speedup | issue util | load | store | fma | reduce | exp2 |
|---|---|---|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|
| softmax_fma_exp_approx | Softmax using a six-FMA polynomial approximation for exp2 when no dedicated EXP2 unit is available. | softmax | 1 | 1 | 12170 | 0.97x | 61.0% | 41.0% | 27.3% | 35.8% | 35.8% | 0.0% |
| softmax_fma_exp_approx | Softmax using a six-FMA polynomial approximation for exp2 when no dedicated EXP2 unit is available. | softmax | 1 | 2 | 11788 | 1.00x | 31.5% | 45.6% | 28.2% | 36.9% | 41.2% | 0.0% |
| softmax_fma_exp_approx | Softmax using a six-FMA polynomial approximation for exp2 when no dedicated EXP2 unit is available. | softmax | 2 | 1 | 9132 | 1.29x | 81.3% | 49.4% | 36.0% | 49.4% | 42.0% | 0.0% |
| softmax_fma_exp_approx | Softmax using a six-FMA polynomial approximation for exp2 when no dedicated EXP2 unit is available. | softmax | 2 | 2 | 6878 | 1.71x | 54.0% | 64.4% | 45.5% | 62.9% | 58.0% | 0.0% |
| softmax_exp2_unit | Softmax using a dedicated EXP2 unit; EXP2 replaces the six dependent FMA polynomial stages. | softmax | 1 | 1 | 11658 | 1.01x | 41.7% | 42.8% | 28.5% | 7.7% | 37.3% | 25.3% |
| softmax_exp2_unit | Softmax using a dedicated EXP2 unit; EXP2 replaces the six dependent FMA polynomial stages. | softmax | 1 | 2 | 11276 | 1.05x | 21.6% | 47.6% | 29.5% | 7.9% | 43.1% | 26.1% |
| softmax_exp2_unit | Softmax using a dedicated EXP2 unit; EXP2 replaces the six dependent FMA polynomial stages. | softmax | 2 | 1 | 7277 | 1.62x | 66.8% | 61.5% | 43.5% | 12.3% | 53.6% | 36.7% |
| softmax_exp2_unit | Softmax using a dedicated EXP2 unit; EXP2 replaces the six dependent FMA polynomial stages. | softmax | 2 | 2 | 5680 | 2.08x | 42.8% | 74.2% | 53.1% | 15.8% | 61.4% | 50.8% |
| rmsnorm | RMSNorm is modeled as per-row input, reduction, rescale, shared weight load, and output store. | rmsnorm | 1 | 1 | 7434 | 0.97x | 49.9% | 44.8% | 22.4% | 36.2% | 29.3% | 0.0% |
| rmsnorm | RMSNorm is modeled as per-row input, reduction, rescale, shared weight load, and output store. | rmsnorm | 1 | 2 | 7180 | 1.00x | 25.8% | 51.7% | 23.2% | 42.7% | 37.4% | 0.0% |
| rmsnorm | RMSNorm is modeled as per-row input, reduction, rescale, shared weight load, and output store. | rmsnorm | 2 | 1 | 4958 | 1.45x | 74.9% | 60.6% | 32.7% | 55.0% | 39.1% | 0.0% |
| rmsnorm | RMSNorm is modeled as per-row input, reduction, rescale, shared weight load, and output store. | rmsnorm | 2 | 2 | 3622 | 1.98x | 51.2% | 95.3% | 46.0% | 69.1% | 66.1% | 0.0% |
| silu_fma_exp_approx | SiLU using a six-FMA polynomial approximation for the exponent stage. | silu | 1 | 1 | 6410 | 0.92x | 87.9% | 26.0% | 26.0% | 77.9% | 0.0% | 0.0% |
| silu_fma_exp_approx | SiLU using a six-FMA polynomial approximation for the exponent stage. | silu | 1 | 2 | 5904 | 1.00x | 47.7% | 32.5% | 32.5% | 88.9% | 0.0% | 0.0% |
| silu_fma_exp_approx | SiLU using a six-FMA polynomial approximation for the exponent stage. | silu | 2 | 1 | 5982 | 0.99x | 94.1% | 26.9% | 27.7% | 83.3% | 0.0% | 0.0% |
| silu_fma_exp_approx | SiLU using a six-FMA polynomial approximation for the exponent stage. | silu | 2 | 2 | 5172 | 1.14x | 54.4% | 35.5% | 33.7% | 94.7% | 0.0% | 0.0% |
| silu_exp2_unit | SiLU using a dedicated EXP2 unit; EXP2 replaces the six dependent FMA polynomial stages. | silu | 1 | 1 | 5898 | 1.00x | 52.1% | 28.2% | 28.2% | 39.1% | 0.0% | 49.9% |
| silu_exp2_unit | SiLU using a dedicated EXP2 unit; EXP2 replaces the six dependent FMA polynomial stages. | silu | 1 | 2 | 5392 | 1.09x | 28.5% | 35.6% | 35.6% | 51.0% | 0.0% | 59.3% |
| silu_exp2_unit | SiLU using a dedicated EXP2 unit; EXP2 replaces the six dependent FMA polynomial stages. | silu | 2 | 1 | 4136 | 1.43x | 74.3% | 38.0% | 43.0% | 54.5% | 0.0% | 58.0% |
| silu_exp2_unit | SiLU using a dedicated EXP2 unit; EXP2 replaces the six dependent FMA polynomial stages. | silu | 2 | 2 | 2712 | 2.18x | 56.6% | 54.1% | 54.3% | 79.1% | 0.0% | 77.7% |
| rope | RoPE mixes shared theta data with per-row input and output streams. | rope | 1 | 1 | 5130 | 0.95x | 59.9% | 64.9% | 32.4% | 44.9% | 0.0% | 0.0% |
| rope | RoPE mixes shared theta data with per-row input and output streams. | rope | 1 | 2 | 4876 | 1.00x | 31.5% | 78.7% | 34.1% | 55.1% | 0.0% | 0.0% |
| rope | RoPE mixes shared theta data with per-row input and output streams. | rope | 2 | 1 | 3838 | 1.27x | 80.0% | 71.5% | 45.9% | 59.0% | 0.0% | 0.0% |
| rope | RoPE mixes shared theta data with per-row input and output streams. | rope | 2 | 2 | 2452 | 1.99x | 62.6% | 99.2% | 54.8% | 86.0% | 0.0% | 0.0% |

## Instruction Mix

| case | description | kernel | lanes | issue width | instructions | micro-ops |
|---|---|---|---:|---:|---:|---:|
| softmax_fma_exp_approx | Softmax using a six-FMA polynomial approximation for exp2 when no dedicated EXP2 unit is available. | softmax | 1 | 1 | 1792 | 7424 |
| softmax_fma_exp_approx | Softmax using a six-FMA polynomial approximation for exp2 when no dedicated EXP2 unit is available. | softmax | 1 | 2 | 1792 | 7424 |
| softmax_fma_exp_approx | Softmax using a six-FMA polynomial approximation for exp2 when no dedicated EXP2 unit is available. | softmax | 2 | 1 | 1792 | 7424 |
| softmax_fma_exp_approx | Softmax using a six-FMA polynomial approximation for exp2 when no dedicated EXP2 unit is available. | softmax | 2 | 2 | 1792 | 7424 |
| softmax_exp2_unit | Softmax using a dedicated EXP2 unit; EXP2 replaces the six dependent FMA polynomial stages. | softmax | 1 | 1 | 1152 | 4864 |
| softmax_exp2_unit | Softmax using a dedicated EXP2 unit; EXP2 replaces the six dependent FMA polynomial stages. | softmax | 1 | 2 | 1152 | 4864 |
| softmax_exp2_unit | Softmax using a dedicated EXP2 unit; EXP2 replaces the six dependent FMA polynomial stages. | softmax | 2 | 1 | 1152 | 4864 |
| softmax_exp2_unit | Softmax using a dedicated EXP2 unit; EXP2 replaces the six dependent FMA polynomial stages. | softmax | 2 | 2 | 1152 | 4864 |
| rmsnorm | RMSNorm is modeled as per-row input, reduction, rescale, shared weight load, and output store. | rmsnorm | 1 | 1 | 896 | 3712 |
| rmsnorm | RMSNorm is modeled as per-row input, reduction, rescale, shared weight load, and output store. | rmsnorm | 1 | 2 | 896 | 3712 |
| rmsnorm | RMSNorm is modeled as per-row input, reduction, rescale, shared weight load, and output store. | rmsnorm | 2 | 1 | 896 | 3712 |
| rmsnorm | RMSNorm is modeled as per-row input, reduction, rescale, shared weight load, and output store. | rmsnorm | 2 | 2 | 896 | 3712 |
| silu_fma_exp_approx | SiLU using a six-FMA polynomial approximation for the exponent stage. | silu | 1 | 1 | 1408 | 5632 |
| silu_fma_exp_approx | SiLU using a six-FMA polynomial approximation for the exponent stage. | silu | 1 | 2 | 1408 | 5632 |
| silu_fma_exp_approx | SiLU using a six-FMA polynomial approximation for the exponent stage. | silu | 2 | 1 | 1408 | 5632 |
| silu_fma_exp_approx | SiLU using a six-FMA polynomial approximation for the exponent stage. | silu | 2 | 2 | 1408 | 5632 |
| silu_exp2_unit | SiLU using a dedicated EXP2 unit; EXP2 replaces the six dependent FMA polynomial stages. | silu | 1 | 1 | 768 | 3072 |
| silu_exp2_unit | SiLU using a dedicated EXP2 unit; EXP2 replaces the six dependent FMA polynomial stages. | silu | 1 | 2 | 768 | 3072 |
| silu_exp2_unit | SiLU using a dedicated EXP2 unit; EXP2 replaces the six dependent FMA polynomial stages. | silu | 2 | 1 | 768 | 3072 |
| silu_exp2_unit | SiLU using a dedicated EXP2 unit; EXP2 replaces the six dependent FMA polynomial stages. | silu | 2 | 2 | 768 | 3072 |
| rope | RoPE mixes shared theta data with per-row input and output streams. | rope | 1 | 1 | 768 | 3072 |
| rope | RoPE mixes shared theta data with per-row input and output streams. | rope | 1 | 2 | 768 | 3072 |
| rope | RoPE mixes shared theta data with per-row input and output streams. | rope | 2 | 1 | 768 | 3072 |
| rope | RoPE mixes shared theta data with per-row input and output streams. | rope | 2 | 2 | 768 | 3072 |

## Case Notes

- `softmax_fma_exp_approx`
  - description: Softmax using a six-FMA polynomial approximation for exp2 when no dedicated EXP2 unit is available.
  - parameters:
    - num_heads: number of independent attention heads to simulate.
    - seq_chunk_bits: input size per head; larger values increase the number of vector-sized chunks per head.
    - exp2_unit: false selects the FMA polynomial path. These six FMA instructions are an implementation alternative to the single EXP2 instruction in softmax_exp2_unit.
    - fma_exp_dependencies: I03 depends on I02; I04 depends on I03; I05 depends on I04; I06 depends on I05; I07 depends on I06; I08 depends on I07.
    - exp_result_fanout: I08 is the exp result. I09 STORE(exp scratch) and I10 REDUCE(sum exp) both depend on I08.
    - final_div_dependencies: I11 LOAD(exp scratch) depends on I09. I12 FMA(div by sum) depends on I11 and on every I10 sum-reduction chunk. I13 STORE(output) depends on I12.
    - lanes: number of independent contexts that can issue in parallel.
    - issue_width: maximum uops issued per cycle across all lanes.
    - lane_stride_0: per-lane input stride for vlane0 loads.
    - lane_stride_1: per-lane output stride for vlane1 stores.
    - lane_stride_3: scratch stride for the exp-reload path used in this softmax model.
  - dependency chain: LOAD(input) -> REDUCE(max) -> LOAD(subtract) -> FMA(poly1) -> FMA(poly2) -> FMA(poly3) -> FMA(poly4) -> FMA(poly5) -> FMA(poly6) -> REDUCE(sum) + STORE(exp scratch) -> LOAD(exp scratch) -> FMA(div) -> STORE(output)
  - single-chain latency diagram:
    ```text
    One softmax head, one vector chunk, exp2_unit=false, 14 vector instructions.
    Stage-level critical path estimate: 92 cycles.
    
    cycle 000-010  I00 LOAD(input)              latency 10
    cycle 010-024  I01 REDUCE(max)              latency 14
    cycle 024-034  I02 LOAD(input/sub source)   latency 10
    cycle 034-038  I03 FMA(exp poly 1)          latency 4
    cycle 038-042  I04 FMA(exp poly 2)          latency 4
    cycle 042-046  I05 FMA(exp poly 3)          latency 4
    cycle 046-050  I06 FMA(exp poly 4)          latency 4
    cycle 050-054  I07 FMA(exp poly 5)          latency 4
    cycle 054-058  I08 FMA(exp poly 6)          latency 4
    cycle 058-072  I10 REDUCE(sum exp)          latency 14
    cycle 058-068  I09 STORE(exp scratch)       latency 10
    cycle 068-078  I11 LOAD(exp scratch)        latency 10
    cycle 078-082  I12 FMA(div by sum)          latency 4, waits for I10 and I11
    cycle 082-092  I13 STORE(output)            latency 10
    ```
- `softmax_exp2_unit`
  - description: Softmax using a dedicated EXP2 unit; EXP2 replaces the six dependent FMA polynomial stages.
  - parameters:
    - num_heads: number of independent attention heads to simulate.
    - seq_chunk_bits: input size per head; larger values increase the number of vector-sized chunks per head.
    - exp2_unit: true selects the dedicated EXP2 path. I08 EXP2 replaces I03-I08 from softmax_fma_exp_approx and produces the same logical exp result for downstream users.
    - exp2_dependency: I08 EXP2 depends on I02 LOAD(input/sub source), which already waits for all max reductions for this head.
    - exp_result_fanout: I09 STORE(exp scratch) and I10 REDUCE(sum exp) both depend on I08.
    - final_div_dependencies: I11 LOAD(exp scratch) depends on I09. I12 FMA(div by sum) depends on I11 and on every I10 sum-reduction chunk. I13 STORE(output) depends on I12.
    - lanes: number of independent contexts that can issue in parallel.
    - issue_width: maximum uops issued per cycle across all lanes.
    - lane_stride_0: per-lane input stride for vlane0 loads.
    - lane_stride_1: per-lane output stride for vlane1 stores.
    - lane_stride_3: scratch stride for the exp-reload path used in this softmax model.
  - dependency chain: LOAD(input) -> REDUCE(max) -> LOAD(subtract) -> EXP2 -> REDUCE(sum) + STORE(exp scratch) -> LOAD(exp scratch) -> FMA(div) -> STORE(output)
  - single-chain latency diagram:
    ```text
    One softmax head, one vector chunk, exp2_unit=true, 9 vector instructions.
    Stage-level critical path estimate: 88 cycles.
    
    cycle 000-010  I00 LOAD(input)              latency 10
    cycle 010-024  I01 REDUCE(max)              latency 14
    cycle 024-034  I02 LOAD(input/sub source)   latency 10
    cycle 034-054  I08 EXP2(x - max)            latency 20
    cycle 054-068  I10 REDUCE(sum exp)          latency 14
    cycle 054-064  I09 STORE(exp scratch)       latency 10
    cycle 064-074  I11 LOAD(exp scratch)        latency 10
    cycle 074-078  I12 FMA(div by sum)          latency 4, waits for I10 and I11
    cycle 078-088  I13 STORE(output)            latency 10
    ```
- `rmsnorm`
  - description: RMSNorm is modeled as per-row input, reduction, rescale, shared weight load, and output store.
  - parameters:
    - num_rows: number of independent rows to simulate.
    - lanes: number of independent contexts that can issue in parallel.
    - issue_width: maximum uops issued per cycle across all lanes.
    - lane_stride_0: per-row input stride for vlane0 loads.
    - lane_stride_1: per-row output stride for vlane1 stores.
    - lane_stride_2: shared weight stride; 0 means every lane reads the same weight block.
  - dependency chain: LOAD(input) -> FMA(square) -> REDUCE(sum) -> FMA(scale) -> LOAD(weight shared) -> FMA(mul) -> STORE(output)
  - single-chain latency diagram:
    ```text
    One RMSNorm row, 7 vector instructions.
    Stage-level critical path estimate: 56 cycles.
    
    cycle 000-010  I00 LOAD(input)              latency 10
    cycle 010-014  I01 FMA(square)             latency 4
    cycle 014-028  I02 REDUCE(sum squares)     latency 14
    cycle 028-032  I03 FMA(scale by rms)       latency 4
    cycle 032-042  I04 LOAD(weight shared)     latency 10, vlane2 stride 0
    cycle 042-046  I05 FMA(multiply weight)    latency 4
    cycle 046-056  I06 STORE(output)           latency 10
    ```
- `silu_fma_exp_approx`
  - description: SiLU using a six-FMA polynomial approximation for the exponent stage.
  - parameters:
    - num_rows: number of independent rows to simulate.
    - exp2_unit: false selects the FMA polynomial exponent path. These six FMA instructions are an implementation alternative to the single EXP2 instruction in silu_exp2_unit.
    - fma_exp_dependencies: I02 depends on I01; I03 depends on I02; I04 depends on I03; I05 depends on I04; I06 depends on I05; I07 depends on I06.
    - lanes: number of independent contexts that can issue in parallel.
    - issue_width: maximum uops issued per cycle across all lanes.
    - lane_stride_0: per-row input stride for vlane0 loads.
    - lane_stride_1: per-row output stride for vlane1 stores.
  - dependency chain: LOAD(input) -> FMA(negate) -> FMA(poly1) -> FMA(poly2) -> FMA(poly3) -> FMA(poly4) -> FMA(poly5) -> FMA(poly6) -> FMA(add1) -> FMA(div/mul) -> STORE(output)
  - single-chain latency diagram:
    ```text
    One SiLU row, exp2_unit=false, 11 vector instructions.
    Stage-level critical path estimate: 56 cycles.
    
    cycle 000-010  I00 LOAD(input)              latency 10
    cycle 010-014  I01 FMA(negate)             latency 4
    cycle 014-018  I02 FMA(exp poly 1)         latency 4
    cycle 018-022  I03 FMA(exp poly 2)         latency 4
    cycle 022-026  I04 FMA(exp poly 3)         latency 4
    cycle 026-030  I05 FMA(exp poly 4)         latency 4
    cycle 030-034  I06 FMA(exp poly 5)         latency 4
    cycle 034-038  I07 FMA(exp poly 6)         latency 4
    cycle 038-042  I08 FMA(add 1.0)            latency 4
    cycle 042-046  I09 FMA(div/mul)            latency 4
    cycle 046-056  I10 STORE(output)           latency 10
    ```
- `silu_exp2_unit`
  - description: SiLU using a dedicated EXP2 unit; EXP2 replaces the six dependent FMA polynomial stages.
  - parameters:
    - num_rows: number of independent rows to simulate.
    - exp2_unit: true selects the dedicated EXP2 path. I02 EXP2 replaces I02-I07 from silu_fma_exp_approx and produces the same logical exponent result for downstream users.
    - exp2_dependency: I02 EXP2 depends on I01 FMA(negate).
    - lanes: number of independent contexts that can issue in parallel.
    - issue_width: maximum uops issued per cycle across all lanes.
    - lane_stride_0: per-row input stride for vlane0 loads.
    - lane_stride_1: per-row output stride for vlane1 stores.
  - dependency chain: LOAD(input) -> FMA(negate) -> EXP2 -> FMA(add1) -> FMA(div/mul) -> STORE(output)
  - single-chain latency diagram:
    ```text
    One SiLU row, exp2_unit=true, 6 vector instructions.
    Stage-level critical path estimate: 52 cycles.
    
    cycle 000-010  I00 LOAD(input)              latency 10
    cycle 010-014  I01 FMA(negate)             latency 4
    cycle 014-034  I02 EXP2                    latency 20
    cycle 034-038  I03 FMA(add 1.0)            latency 4
    cycle 038-042  I04 FMA(div/mul)            latency 4
    cycle 042-052  I05 STORE(output)           latency 10
    ```
- `rope`
  - description: RoPE mixes shared theta data with per-row input and output streams.
  - parameters:
    - num_rows: number of independent rows to simulate.
    - lanes: number of independent contexts that can issue in parallel.
    - issue_width: maximum uops issued per cycle across all lanes.
    - lane_stride_0: per-row input stride for vlane0 loads.
    - lane_stride_1: per-row output stride for vlane1 stores.
    - lane_stride_2: shared theta stride; 0 means every lane reads the same theta block.
  - dependency chain: LOAD(theta shared) -> FMA(angle) -> LOAD(input) -> FMA(real/imag) -> STORE(output)
  - single-chain latency diagram:
    ```text
    One RoPE row/position block, 6 vector instructions.
    Stage-level critical path estimate: 42 cycles.
    
    cycle 000-010  I00 LOAD(theta shared)       latency 10, vlane2 stride 0
    cycle 010-014  I01 FMA(angle = theta*pos)  latency 4
    cycle 014-024  I02 LOAD(input real/imag)   latency 10
    cycle 024-028  I03 FMA(real output)        latency 4
    cycle 028-032  I04 FMA(imag output)        latency 4
    cycle 032-042  I05 STORE(output)           latency 10
    ```
